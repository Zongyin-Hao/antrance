apply plugin: 'java'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile 'com.android.tools.build:gradle:2.3.3'
}

group = 'xmu.wrxlab'
version = '0.0.1'
//打包上传到本地
uploadArchives{
    repositories {
        mavenDeployer {
            repository(url: uri('/home/hzy/hzy/projects/android/auiauto/database/abuilder'))
        }
    }
}